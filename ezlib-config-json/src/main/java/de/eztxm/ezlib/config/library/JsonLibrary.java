package de.eztxm.ezlib.config.library;

public enum JsonLibrary {
    CUSTOM,     // Use the custom, standalone implementation
    ORG_JSON,   // Use the org.json library
    GSON        // Use the Google Gson library
}
